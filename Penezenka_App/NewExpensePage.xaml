<Page
    x:Class="Penezenka_App.NewExpensePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Penezenka_App"
    xmlns:converters="using:Penezenka_App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding NewExpensePageViewModel, RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:DoubleToStringConverter x:Key="Double2StringConv" />
        <converters:IntToDateTimeConverter x:Key="IntToDateTimeConverter" />
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter" />
    </Page.Resources>

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock x:Uid="Header" Text="PENĚŽENKA" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock x:Uid="NewExpensePageNewTitle" Name="NewExpenseTitle" Text="nový výdaj" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
            <TextBlock x:Uid="NewExpensePageEditTitle" Name="EditExpenseTitle" Text="úprava výdaje" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <ScrollViewer x:Name="ContentRoot" Grid.Row="1" Margin="19,9.5,19,0" Padding="0,0,10,0">
            <StackPanel VerticalAlignment="Top">
                <ComboBox HorizontalAlignment="Stretch" SelectedIndex="-1" Header="Účet / peněženka"/>
                <TextBox Name="RecordName" x:Uid="NewExpenseTitle" Text="{Binding Record.Title, FallbackValue=''}" TextWrapping="Wrap" VerticalAlignment="Top" Header="Název položky" PlaceholderText="název"/>
                <StackPanel Orientation="Horizontal">
                    <DatePicker Name="RecordDate" x:Uid="NewExpenseDate" Date="{Binding Record.Date, Converter={StaticResource IntToDateTimeConverter}, FallbackValue=''}" FontSize="18.667" Header="Datum" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                    <TextBox x:Name="Castka" x:Uid="NewExpenseAmount" Text="{Binding Record.Amount, Converter={StaticResource Double2StringConv}, FallbackValue='', Mode=OneTime}" TextWrapping="Wrap" VerticalAlignment="Top" InputScope="Number" Header="Částka" PlaceholderText="korunačky" Margin="22,0,4,0" HorizontalAlignment="Right" Width="195"/>
                    <TextBlock x:Uid="NewExpenseCurrency" TextWrapping="Wrap" Text="Kč" FontSize="20.26" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,19,0,0"/>
                </StackPanel>
                <TextBox Name="RecordNotes" x:Uid="NewExpenseNotes" Text="{Binding Record.Notes, FallbackValue=''}" TextWrapping="Wrap" VerticalAlignment="Top" Header="Poznámky" AcceptsReturn="True" PlaceholderText="poznámky"/>
                <CheckBox Name="RecordRecurring" IsEnabled="false" Content="Fixní výdaj" HorizontalAlignment="Left" VerticalAlignment="Top" IsChecked="False"/>
                <StackPanel Orientation="Horizontal">
                    <ComboBox HorizontalAlignment="Left" VerticalAlignment="Top" Width="120" Header="Četnost" SelectedIndex="0" IsEnabled="{Binding IsChecked, ElementName=RecordRecurring, Mode=OneWay}">
                        <ComboBoxItem Content="Ročně"/>
                        <ComboBoxItem Content="Měsíčně" IsSelected="True"/>
                        <ComboBoxItem Content="Týdně"/>
                    </ComboBox>
                    <ComboBox HorizontalAlignment="Left" VerticalAlignment="Top" Width="120" Header="Den v měsíci" SelectedIndex="-1" IsEnabled="{Binding IsChecked, ElementName=RecordRecurring, Mode=OneWay}" Margin="10,0,0,0"/>
                </StackPanel>
                <GridView
                    Header="Štítky"
                    ItemsSource="{Binding Tags}"
                    SelectionMode="Multiple" FontSize="20.26" Margin="0,10,0,20">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VariableSizedWrapGrid Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Color, Converter={StaticResource ColorToBrushConverter}}">
                                <TextBlock Text="{Binding Title}" MinWidth="105" Padding="10,7,10,7" FontSize="16" FontWeight="Bold" HorizontalAlignment="Left" />
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                    <GridView.ItemContainerStyle>
                        <Style TargetType="FrameworkElement">
                            <Setter Property="Margin" Value="0,0,7,7"/>
                        </Style>
                    </GridView.ItemContainerStyle>
                </GridView>
            </StackPanel>
        </ScrollViewer>

    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Uid="NewExpenseAppBarSave" x:Name="UlozitAppBarButton" Label="add" Icon="Save" Click="Ulozit_Click" />
            <AppBarButton x:Uid="NewExpenseAppBarCancel" x:Name="StornoAppBarButton" Label="storno" Icon="Cancel" Click="Storno_Click" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>
